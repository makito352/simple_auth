本プロジェクトはポートフォリオとして公開するため、開発者としてのセキュリティ意識を示すことを目的とする。
ただし、企業向けの監査ログや証跡管理など、過剰なセキュリティ強化は行わない。

最低限のセキュリティ品質を担保するため、以下を実施する：
- 認証処理の主要イベントをログとして記録（個人情報・秘密鍵は出力しない）
- 例外発生時は標準化したエラーレスポンスを返し、詳細はサーバー側ログに記録
- WebAuthn/OIDC の失敗理由は分類してハンドリングする
